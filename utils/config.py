# Configuration for training and other parameters
config = {
    "epochs": 10,
    "batch_size": 32,
    "learning_rate": 0.001,
    "image_size": 64  # assumed image width/height used for models
}
